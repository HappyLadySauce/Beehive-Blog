insecure:
    bind-address: 0.0.0.0 # 绑定的不安全 IP 地址，设置为 0.0.0.0 表示使用全部网络接口，默认为 127.0.0.1
    bind-port: 8080 # 提供非安全认证的监听端口，默认为 8080

database:
    host: 127.0.0.1
    port: 5432
    user: Beehive-Blog
    password: Beehive-Blog
    db: Beehive-Blog
    ssl-mode: disable
    max-idle-conns: 10
    max-open-conns: 100
    conn-max-lifetime: 1h
    conn-max-idle-time: 30m

cache:
    host: 127.0.0.1
    port: 6379
    db: 0
