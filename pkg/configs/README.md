# Shared Configs

This directory is reserved for shared configuration structures once go-zero services are generated.

Keep only cross-service config definitions here. Service-local config stays inside each service under `internal/config`.
